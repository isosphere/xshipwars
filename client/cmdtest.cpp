#include "xsw.h"
#include "net.h"

 
int CmdTest(const char *arg)
{
        if(arg == NULL)
            return(-1);
/*
	XSWDoDestroyed(
	    6,
	    net_parms.player_obj_num,
	    6,
	    0
	);
 */
        return(0);
}
